# AI简历助手开发文档

## 项目概述

AI简历助手是一个基于Next.js和Langchain的智能应用，旨在帮助用户通过对话方式与其简历进行交互。系统将能够处理本地简历文件，将其分片向量化，并通过检索增强生成（RAG）技术，使AI模型能够基于简历内容回答用户提出的问题。

## 功能需求

### 核心功能

1. **简历上传与解析**
   - 支持多种格式的简历上传（PDF, DOCX, TXT等）
   - 解析简历内容并提取文本

2. **文本分片处理**
   - 将长简历文档分割成适当大小的片段
   - 确保分片间的语义完整性

3. **向量化存储**
   - 使用嵌入模型将文本片段向量化
   - 将向量存储在内存向量数据库中

4. **检索增强生成**
   - 根据用户问题检索相关简历内容
   - 结合检索结果生成准确回答

5. **对话交互**
   - 提供友好的用户界面进行问答交互
   - 维持对话上下文

### 非功能性需求

1. **性能**
   - 快速响应用户查询
   - 高效处理大型文档

2. **安全性**
   - 用户简历数据隐私保护
   - 数据不持久化存储

3. **可扩展性**
   - 支持多种文档格式
   - 可替换不同AI模型

## 技术架构

### 前端技术栈

- **Next.js 13+** - React框架，提供服务端渲染和API路由
- **TypeScript** - 类型安全
- **Tailwind CSS** - UI样式
- **React Hooks** - 状态管理

### 后端/服务端技术栈

- **Next.js API Routes** - 处理后端逻辑
- **Langchain.js** - 构建LLM应用的框架
- **PDF解析库** (如pdf-parse) - 处理PDF文档
- **内存向量数据库** (如HNSWLib或MemoryVectorStore) - 存储向量嵌入
- **嵌入模型** (如OpenAI Ada或HuggingFace embeddings) - 文本向量化

### 系统架构图

```
┌─────────────────┐    ┌──────────────────┐    ┌──────────────────┐
│   User Frontend │◄──►│   Next.js Server │◄──►│  Memory Storage  │
└─────────────────┘    └──────────────────┘    └──────────────────┘
                              │
                       ┌─────────────┐
                       │  Langchain  │
                       └─────────────┘
                              │
                    ┌──────────────────┐
                    │  LLM (OpenAI)    │
                    └──────────────────┘
```

## 核心模块设计

### 1. 文档处理模块

#### 功能描述
负责接收用户上传的简历文件，解析内容并提取纯文本。

#### 输入输出
- 输入：用户上传的简历文件（PDF/DOCX/TXT）
- 输出：纯文本格式的简历内容

#### 实现要点
- 支持多种文档格式解析
- 文本清洗和预处理
- 错误处理机制

### 2. 文本分片模块

#### 功能描述
将长文本分割成适当大小的片段，以便后续向量化处理。

#### 输入输出
- 输入：纯文本简历内容
- 输出：文本块列表

#### 实现要点
- 使用Langchain的文本分割器（如RecursiveCharacterTextSplitter）
- 设置合适的块大小和重叠度
- 保持语义完整性

### 3. 向量化模块

#### 功能描述
将文本片段转换为向量表示，并存储到内存向量数据库中。

#### 输入输出
- 输入：文本块列表
- 输出：内存中的向量存储索引

#### 实现要点
- 选择合适的嵌入模型
- 构建和存储向量索引到内存中
- 支持增量更新

### 4. 检索模块

#### 功能描述
根据用户问题检索最相关的简历内容片段。

#### 输入输出
- 输入：用户问题
- 输出：相关文本片段

#### 实现要点
- 向量相似度计算
- 相似度阈值设置
- 返回Top-K结果

### 5. 问答模块

#### 功能描述
结合检索到的相关内容和用户问题，生成自然语言回答。

#### 输入输出
- 输入：用户问题 + 相关内容片段
- 输出：AI生成的回答

#### 实现要点
- Prompt工程设计
- 上下文管理
- 回答质量控制

## 数据流设计

### 1. 初始化流程
```
用户上传简历
      ↓
文档解析模块
      ↓
文本分片模块
      ↓
向量化模块
      ↓
存储到内存向量数据库
```

### 2. 查询流程
```
用户提问
      ↓
问题向量化
      ↓
内存向量数据库检索
      ↓
相关文档片段
      ↓
构建Prompt
      ↓
调用LLM生成回答
      ↓
返回给用户
```

## API接口设计

### 1. 上传简历接口
```
POST /api/upload
Content-Type: multipart/form-data

请求体:
- file: 简历文件

响应:
- status: 处理状态
- message: 处理信息
```

### 2. 提问接口
```
POST /api/chat
Content-Type: application/json

请求体:
- question: 用户问题
- sessionId: 会话ID（可选）

响应:
- answer: AI回答
- sources: 引用的简历片段
```

### 3. 清除会话接口
```
DELETE /api/session
Content-Type: application/json

请求体:
- sessionId: 会话ID

响应:
- status: 处理状态
```

## 内存存储方案

### 向量数据库
使用内存向量存储（如HNSWLib或MemoryVectorStore），所有数据仅在会话期间保存在内存中：

| 字段 | 类型 | 描述 |
|------|------|------|
| id | String | 向量ID |
| vector | Array<Float> | 向量数据 |
| metadata | Object | 元数据（如文本内容、页码等） |

### 会话存储
使用内存存储维护会话状态：

| 字段 | 类型 | 描述 |
|------|------|------|
| sessionId | String | 会话ID |
| vectorStore | Object | 内存中的向量存储引用 |
| history | Array | 对话历史 |

### 数据生命周期管理
1. **加载阶段**：用户上传简历时，解析并加载到内存中
2. **使用阶段**：会话期间所有操作都在内存中进行
3. **清理阶段**：会话结束时自动清除所有内存数据

## 安全与隐私考虑

1. **数据隐私**
   - 用户简历仅在会话期间保存在内存中
   - 不将用户数据存储在持久化数据库中
   - 会话结束后自动清除所有数据

2. **访问控制**
   - 确保每个用户的向量存储相互隔离
   - 使用会话ID区分不同用户

3. **输入验证**
   - 验证上传文件类型和大小
   - 过滤恶意内容

## 部署方案

### 开发环境
- Node.js >= 16
- Next.js开发服务器
- 内存向量存储

### 生产环境
- Vercel/Node.js服务器部署
- 内存向量存储（无外部数据库依赖）
- CDN加速静态资源

## 项目目录结构

```
resume-assistant/
├── app/                    # Next.js 13+ app目录
│   ├── api/               # API路由
│   ├── components/        # React组件
│   ├── lib/               # 核心库文件
│   └── pages/             # 页面组件
├── public/                # 静态资源
├── styles/                # 样式文件
├── uploads/               # 上传文件临时目录
├── next.config.js         # Next.js配置
├── tsconfig.json          # TypeScript配置
└── package.json           # 项目依赖
```

## 可能的扩展功能

1. **多语言支持**
   - 支持不同语言的简历处理
   - 多语言问答

2. **简历分析**
   - 提供简历优化建议
   - 技能关键词分析

3. **导出会话**
   - 将问答内容导出为文档

4. **多模态支持**
   - 支持图像简历分析

## 开发计划

1. **第一阶段**：基础架构搭建
   - Next.js项目初始化
   - 文档上传和解析功能
   - 基础UI界面

2. **第二阶段**：核心功能实现
   - 文本分片和向量化
   - 内存向量存储和检索
   - 基础问答功能

3. **第三阶段**：优化和完善
   - UI/UX优化
   - 性能优化
   - 错误处理和测试

4. **第四阶段**：扩展功能
   - 添加额外功能
   - 部署和文档完善